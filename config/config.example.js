// config/config.example.js（提交到Git）
module.exports = {
  // 示例：本地文件路径（仅作为模板，无真实值）
  localFilePath: '/path/to/your/local/file', // 本地文件路径占位符
  // 示例：后端服务路径
  servicePath: '/api/v1/example', // 服务路径示例
  // 示例：数据库路径（仅示例，真实值写在local.js）
  dbPath: '/path/to/your/db',
}
